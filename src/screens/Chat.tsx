import Layout from "@src/components/Layout";
import { View, Text } from "react-native";

const Chat = () => {
  return (
    <Layout>
      <View className="flex items-center justify-center mt-20">
        <Text className="text-8xl text-[#BFBFC0]">02</Text>
      </View>
    </Layout>
  );
};

export default Chat;
